RewriteEngine On

# Si el archivo o carpeta existe, servirlo normalmente
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Si no existe, redirigir todo a React
RewriteRule ^ index.html [L]
